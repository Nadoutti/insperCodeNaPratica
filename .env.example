# Porta expostas (deixe vazio para não expor externamente)
# Útil para produção: não expor portas do banco e redis
EXPOSE_POSTGRES_PORT=5432
EXPOSE_REDIS_PORT=6379
EXPOSE_APP_PORT=5000

# SMTP (obrigatório para envio de emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Webhook do Tally
TALLY_WEBHOOK_SECRET=your-webhook-secret

# Flask (opcional - tem valores padrão)
SECRET_KEY=your-secret-key-here
